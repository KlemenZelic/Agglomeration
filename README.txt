1.) save all the files to the same directory
2.) open main.c and ply with the parameters at the beggining of the code
3.) save, compile and run main.c (will not work if all .txt fies are not in the same folder)
4.) two new files will be generated during execution of main: data.txt and parameters.txt. These are the results
5.) use Wolfram Mathemtaica to open Plot Agglomerat.nb script to plot the constructed agglomerate 